import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});
import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});iimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});pimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});{import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});aimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});}import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});fimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});'import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});jimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});aimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});iimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});'import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});;import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});
import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});iimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});pimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});{import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});Uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});}import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});fimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});'import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});.import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});.import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});/import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});yimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});pimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});/import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});'import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});;import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});
import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});
import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});ximport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});pimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});cimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});nimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});Aimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});=import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});aimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});timport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});oimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});mimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});<import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});Uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});simport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});eimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});rimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});|import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
}); import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});nimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});limport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});limport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});>import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});(import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});nimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});uimport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});limport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});limport { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});)import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});;import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});
import { atom } from 'jotai';

export type UserRole = 'SUPER_ADMIN' | 'ORG_ADMIN' | 'HR_MANAGER' | 'DEPT_MANAGER' | 'EMPLOYEE';

export interface User {
  id: string;
  role: UserRole;
  name: string;
  email: string;
}

export const userAtom = atom<User | null>({
  id: '1',
  role: 'ORG_ADMIN',
  name: 'Admin User',
  email: 'admin@example.com'
});